# Plan: Add Profile Card to Organizer Dashboard

## Goal
Add a profile summary card to the organizer dashboard (`organizer/index.php`) showing the user's avatar, name, email, and role — matching the style of the player profile card.

## Changes

### 1. `organizer/index.php`
- Fetch the user's account data from the `users` table (email, username, role, auth_method)
- Add a profile summary card at the top of the page (above the stats grid) with:
  - Gradient avatar circle with the user's first initial
  - Display name (username or real name if available)
  - Email and role info
  - Matches the style from `player/profile.php` lines 104-126

### 2. `includes/header.php` (line 114)
- Fix the hardcoded profile link in the sidebar footer — currently always points to `player/profile.php` even for organizers/admins
- Update to point to the organizer's dashboard profile section or keep the dashboard as the default

## Files to Modify
- `C:\xampp\htdocs\TournamentHQ\organizer\index.php` — add profile card + user data query
- `C:\xampp\htdocs\TournamentHQ\includes\header.php` — fix sidebar profile link for organizer role

## Verification
- Log in as an organizer and visit the dashboard
- Confirm the profile card displays with avatar, name, email, and role
- Confirm sidebar avatar link navigates correctly
